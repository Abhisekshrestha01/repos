@model IEnumerable<mvcCompleteProject.Models.Lecture>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Subject)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lecturer)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LectureDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lecturer)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LectureDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LectureId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LectureId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LectureId })
        </td>
    </tr>
}

</table>
